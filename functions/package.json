{
  "name": "pi-lottery-functions",
  "description": "Firebase Functions for Pi Lottery Platform v2.0",
  "version": "2.0.0",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "clean": "rm -rf node_modules package-lock.json && npm install",
    "build": "echo 'No build step required for Node.js functions'",
    "test": "echo 'No tests configured'"
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "cors": "^2.8.5",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  },
  "private": true,
  "author": "Pi Lottery Team",
  "license": "MIT",
  "keywords": [
    "firebase",
    "functions",
    "pi-network",
    "lottery",
    "payments",
    "blockchain"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yss1235-why/pi-lottery-admin.git"
  }
}
